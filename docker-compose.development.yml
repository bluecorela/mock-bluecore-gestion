services:
  api:
    build:
      context: .
      target: builder
    image: bluecore-gestion-api:development
    command: npm run start:dev
    environment:
      NODE_ENV: development
    read_only: false
    volumes:
      - .:/app
      - api_node_modules:/app/node_modules

volumes:
  api_node_modules:
